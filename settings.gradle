def root = rootProject.projectDir

def list = root.listFiles()

for (f in list) {
  if (f.isDirectory() && !f.name.startsWith('.')){
    if (new File(f, 'vertx.properties').canRead()) {
      println "Including Project: ${f.name}"
      include f.name
    }
  }
}